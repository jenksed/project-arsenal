interface:
  display_name: "Project Arsenal"
  short_description: "Repo-specific patterns and workflows for project-arsenal"
  default_prompt: "Use the project-arsenal repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true